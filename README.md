# Fractal

- use `cargo run --bin fractal-editor` to run the code editor ui
- use `cargo run --bin fractal-compiler path/to/file.fr` to run the compiler

files
constants and math for checking import file cases
main: basics
operatorTest: check all operators cases
dataStructures : struct, list and array cases
errors: deliberate edge case errors
semanter_check: semanter cases for theerttha