#![allow(unused_variables, unused_mut, dead_code, non_snake_case, unused_imports, unreachable_patterns)]
use std::io::{self, BufRead, Write};

fn main() {
    let mut x: i64 = 10_i64;
    let mut y: f64 = 8.0_f64;
    let mut z: i64 = (x + (y as i64));
}
