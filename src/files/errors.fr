!start

    :int n = 0;
    !if (n == 0) {
        # type error
        :int x = 4.5;
    } !else if(n == 1) {
        #using undeclared variable
        :int a = c;
    } !else !(n == 2) {
        !func add(a : int, b : int) -> int {
            return a + b;
        }
        !func add(b : int) -> int {
            return b;
        }

    }

!end
