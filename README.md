# Fractal

- use `cargo run --bin fractal-editor` to run the code editor ui
- use `cargo run --bin fractal-compiler path/to/file.fr` to run the compiler

- cargo build --release
- cargo install --path .
- add to path
- fractal path/to/file.fr to compile the program
- ./path/to/file to execute the program