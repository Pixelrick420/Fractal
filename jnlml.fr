!start
    # code here
    :int n;
    input("{}", n);
    :int n2 = n;
    :int mul = 0;
    :int temp;
    !while (n2 > 0) {
        temp = n2 % 10;
        mul += temp * temp * temp;
        n2 /= 10;
    }
    !if (mul == n) {
        print("armstrong\n");
    }
    !else {
        printf("not\n");
    }
!end
