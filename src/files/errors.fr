!start

    :int n = 0;
    !if (n == 0) {
        # type error
        :nt x = 4.5;
    } !else if(n == 1) {
        #usng undeclared varable
        :nt a = c;
    } !else !(n == 2) {
        !func add(a : nt, b : nt) -> nt {
            return a + b;
        }
        !func add(b : nt) -> nt {
            return b;
        }

    }

!end
